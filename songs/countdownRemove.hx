currentSong = PlayState.SONG.meta.name.toLowerCase();

function create() introLength = 0;

function onCountdown(event:CountdownEvent) event.cancel();

function onStrumCreation(_) _.__doAnimation = false;